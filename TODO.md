# TODO

- [ ] Icoon voor de "Studentikoos"-kaart vervangen door een echte das/verenigingsdas-SVG (Phosphor heeft geen stropdas-icoon; nu tijdelijk een `coat-hanger`-icoon).
- [ ] Echte hero-foto toevoegen op de homepage (staat nog op een tijdelijke stockfoto van Unsplash) — kan sinds 2026-09-24 zelf via **Homepage → Bovenaan de pagina → Foto**. De "Over ons"-sectie is op 2026-09-23 al vervangen door een echte foto (`images/photos/verenigingsfotos/verenigingsfoto-2025.jpg`, EJW 2025) — dat was een eigen inschatting welke van de twee stockfoto's deze foto het beste verving, zeg het als de hero-plek toch beter had gepast.
- [ ] Instagram-link van VGSD invullen: `social.instagram` in `data/site.json` (staat nu leeg, het icoon in de footer wijst dan naar "#"). Het WhatsApp-icoon in de footer is al gekoppeld aan het WhatsApp-nummer. Adres, e-mail en telefoon zijn ingevuld.
- [ ] **Centrale gegevens (sinds 2026-09-21, stap 1 van de beheerpagina)**: WhatsApp-nummer, contactgegevens (adres, e-mail, pr-e-mail, telefoon), Instagram-link, de kalender met open avonden en de besturen staan niet meer in de HTML maar in `data/site.json`, `data/activiteiten.json` en `data/besturen.json`. `js/site.js` leest die en vult ze op alle pagina's in. **Pagina's moeten nu via http(s) bekeken worden (preview-site of lokale server), niet door het bestand dubbel te klikken (`file://`)**, anders kunnen de databestanden niet geladen worden. Volgende stappen: beheerpagina met formulieren (Sveltia CMS) en een bijsnij-tool voor foto's.
- [x] **Inloggen op `/admin/` getest en werkend (2026-09-22)**: zowel met een toegangstoken als met de knop "Inloggen via GitHub" (zie stap hieronder voor hoe dat laatste is opgelost). Opslaan, de formulieren en de foto-upload werken.
- [ ] **Beheerpagina (Sveltia CMS) — stap 2 gebouwd op 2026-09-21, nog niet door jou getest**: `admin/index.html` + `admin/config.yml` + handleiding `BEHEER.md`. Formulieren voor Besturen (nieuw bestuur bovenaan toevoegen), Instellingen (WhatsApp, adres, e-mails, telefoon, Instagram) en Open avonden. Sveltia 0.217.0 (MIT, gratis) staat vast met controlesom (SRI). Gecontroleerd: `/admin/` laadt en accepteert de config (inlogscherm met repo-naam zichtbaar); `config.yml` is geldig volgens het officiële Sveltia-schema (en het schema keurt een foutieve versie wel af). **Niet getest (kon ik niet zonder token): inloggen, het opslaan van een wijziging, de formulieren zelf, en de foto-upload.** Test dit als eerste: maak een fine-grained token (stappen in `BEHEER.md`), open `/admin/`, wijzig iets kleins (bv. Instagram-link) en kijk of de commit verschijnt en de site na ~2 min bijwerkt. Verder:
  - **Stap 3 gebouwd (2026-09-21): sleep-en-bijsnij-veld** `admin/vgsd-foto.js` (`CMS.registerFieldType('vgsd-foto', …)`, geschreven met Sveltia's `createClass`/`h`, geen losse React). Uploaden of naar het veld slepen → foto wordt in de browser verkleind (max 1600px, JPEG 86%) en met `addFile` meegecommit; verslepen in een kader (verhouding van het kaartje) stelt de uitlijning in, met live voorbeelden voor het grote blok en de popup, pijltjestoetsen, "Uitlijning herstellen", "Bestaande foto kiezen", "Foto verwijderen" en een voorbeeld in het rechterpaneel. **Datamodel gewijzigd**: `foto` is nu een object `{ "src": "/images/...", "positie": "50% 30%" }` (het aparte veld `fotoPositie` bestaat niet meer); `besturen.html` leest dat via `fotoVan()` (accepteert nog een kale tekst als pad). Getest in Sveltia's testmodus (`test-repo`, geen echte GitHub) met de definitieve `config.yml`: uploaden, verkleinen naar 1600×1067, slepen, opslaan, heropenen (pad vervangen door `/images/photos/besturen/<slug>.jpg`, uitlijning bewaard), en het objectveld overleeft lijst-bewerkingen en opslaan/heropenen (het bekende probleem #969 deed zich niet voor in 0.217.0). **Niet getest**: echte touch-bediening (slepen met een vinger), zeer grote foto's (>20 MB), HEIC (wordt afgekeurd met een melding), en alles via een echte GitHub-login. Bekende beperking: de foto is pas in het beheerscherm zichtbaar zodra de site na het opslaan is bijgewerkt (~1-2 min); tot die tijd staat er een uitlegtekst. Het kader toont de verhouding van het kaartje (2,03:1); de foto wordt op de site ook in andere verhoudingen getoond (groot blok ≈1,17:1, popup ≈2,1:1), daarom de twee kleine voorbeelden.
  - **Inloggen met de knop "Sign In with GitHub" is nu ook opgelost (2026-09-22)**: hiervoor is een gratis Cloudflare Worker aangemaakt (naam `vgsd-cms-auth`, URL `https://vgsd-cms-auth.webmaster-484.workers.dev`) die als OAuth-tussenstap dient — nodig omdat GitHub Pages, anders dan Netlify, geen ingebouwde OAuth-provider heeft. De Worker draait de open-source code van `sveltia-cms-auth` (MIT-licentie), rechtstreeks geplakt in de Cloudflare-dashboard-code-editor (geen losse GitHub-repo/CI, dus geen `wrangler.toml`/CLI om bij te houden — de melding "Update your Wrangler configuration" die daarbij verschijnt mag genegeerd worden). Bijbehorende **GitHub OAuth App** ("VGSD Beheer Login") staat onder Settings → Developer settings → **OAuth Apps** (niet "GitHub Apps", die hebben een ander formulier) op het `webmasterVGSD`-account, met environment-variabelen `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` (secret) en `ALLOWED_DOMAINS=preview.vgsd.nl` bij de Worker, en `base_url` in `admin/config.yml` die naar de Worker-URL wijst. **Beveiliging gecontroleerd en in orde**: de repo heeft 0 collaborators ("Only you can contribute to this repository"), dus alleen het `webmasterVGSD`-account kan daadwerkelijk iets opslaan via de beheerpagina — een ander GitHub-account kan wel het inlogscherm zien (dat is normaal, geen lek), maar niet daadwerkelijk wijzigingen opslaan.
  - Wijzigingen zijn na opslaan pas na ~1-5 minuten zichtbaar op de site (GitHub Pages moet herbouwen + GitHub's CDN-cache moet doorstromen) — dit is normaal, geen storing. Bij twijfel: tabblad "Actions" van de repo checken op een gelukte "pages build and deployment", en een harde herlaad (Ctrl+Shift+R) doen in de browser.
  - De twee kleine voorbeeldkadertjes in het foto-veld ("Groot uitgelicht blok" / "Popup") laten met opzet dezelfde foto/uitlijning zien als het grote sleepkader erboven, puur ter controle dat er niets wegvalt — hier is inmiddels een uitlegzin bij gezet in `admin/vgsd-foto.js`, want dat was niet vanzelfsprekend.
  - **Foto-paden zijn nu absoluut vanaf de hoofdmap** (`/images/photos/besturen/x.jpg`, in `data/besturen.json`), passend bij `media_folder` in `admin/config.yml`. Werkt op `preview.vgsd.nl` en lokaal via `site-bekijken.bat`; zou breken als de site ooit in een submap wordt gehost.
  - Het beheerscherm is in de taal van je browser; of Sveltia Nederlands als taal ondersteunt heb ik niet gecontroleerd.
- [x] **Beheerpagina uitgebreid: Activiteiten en Geschiedenis volledig via de CMS bewerkbaar (2026-09-23, op jouw verzoek)**: elke pagina heeft nu een eigen ingang in het menu links van de beheerpagina (Activiteiten, Open avonden, Besturen, Geschiedenis, Instellingen — dezelfde volgorde als het menu op de site).
  - **`activiteiten.html` is niet meer statische HTML**: de tekst bovenaan en alle activiteitenkaarten (titel, icoon, foto, korte omschrijving, "Lees meer"-tekst, rood/wit, volgorde) staan nu in `data/activiteiten-overzicht.json` en worden door een script onderaan `activiteiten.html` opgebouwd. Toevoegen/verwijderen/herschikken van kaarten kan volledig via **Activiteiten → Activiteitenpagina** in de beheerpagina (lijst-widget met sleephandvat). Bij "Rood uitgelicht" staat een hint om rode kaarten niet te clusteren (zie de eerdere feedback hierover).
  - **Foto-bijsnijveld voor activiteiten** (`admin/vgsd-foto-activiteiten.js`, veldtype `vgsd-foto-activiteiten`): zelfde upload/sleep/bijsnij-werking als het besturen-veld (`admin/vgsd-foto.js`), maar met een eigen, bredere kader-verhouding (2,4:1, passend bij de foto's op een activiteitenkaart) en zonder de "groot uitgelicht blok"/"popup"-extra's die alleen voor besturen gelden. Bewust een apart bestand i.p.v. het besturen-veld hergebruiken/aanpassen, om niets te riskeren aan dat al-werkende veld. Foto's komen in `images/photos/Activiteiten/` (`media_folder`/`public_folder` op het veld zelf, los van de standaardmap voor besturen).
  - **`geschiedenis.html` ook niet meer statisch**: tekst bovenaan en de hele tijdlijn (periode, titel, tekst, en welke stap "huidig" is) staan in `data/geschiedenis.json`, opgebouwd door een script onderaan de pagina. Nieuwe tijdlijnstappen toevoegen/verwijderen/herschikken kan via **Geschiedenis → Geschiedenispagina**.
  - **Getest in Sveltia's testmodus** (`test-repo`, geen echte GitHub, zoals eerder bij het besturen-veld): alle vijf collecties laden zonder fouten, de nieuwe kaarten-/tijdlijn-lijsten kunnen aangevuld worden, en het nieuwe fotoveld accepteert een upload en toont het bijsnijkader op de juiste breedte-hoogteverhouding. **Niet getest**: opslaan via een echte GitHub-commit (kon niet zonder de productie-Worker te gebruiken) en het slepen om bij te snijden zelf (de sleepmechaniek is ongewijzigd overgenomen uit het al-geteste besturen-veld, dus het risico daarop is laag).
  - De publieke pagina's (`activiteiten.html`, `geschiedenis.html`) zijn met Playwright vergeleken vóór/na de omzetting naar data-gestuurd: pixelidentiek, geen consolefouten, "Lees meer"-accordions werken.
  - ~~Nog niet gedaan: verbanden, kasten, veelgestelde vragen, homepage, open avonden~~ → **gedaan op 2026-09-24**, zie het punt "Hele site via de beheerpagina" hieronder.
- [x] **VVGSD (voetbalteam): drie nieuwe pagina's + automatische scraper (2026-09-23, op jouw verzoek)**: `vvgsd.html` (hub met groepsfoto, eerstvolgende wedstrijd, topscorers, wedstrijdverslagen), `wedstrijdschema.html` (volledig programma) en `stand.html` (competitiestand). Nav op alle pagina's gekoppeld (was een "#"-placeholder onder "Onderdelen van de vereniging").
  - **Stand en wedstrijdschema komen automatisch van Playpass** (de plek waar de TU Delft-studentencompetitie het bijhoudt, niet de KNVB), zoals gevraagd — geen handmatige invoer nodig zolang dat werkt. `scripts/vvgsd-scraper.mjs` haalt de standen- en schemapagina op (gewone server-rendered HTML, geen headless browser nodig) en schrijft `data/vvgsd-stand-auto.json` + `data/vvgsd-schema-auto.json`. Draait elke 3 uur via `.github/workflows/vvgsd-scraper.yml` (GitHub Action) en commit alleen als er iets veranderd is.
  - **Kolom-koppeling is aanpasbaar zonder code-wijziging, zoals gevraagd**: welk veld (bv. "punten") bij welke koptekst op Playpass hoort (bv. "Points"), staat in de beheerpagina onder **VVGSD → Stand en schema → Koppeling kolomkoppen op Playpass**, niet in de scraper-code zelf. Hernoemt Playpass een koptekst, dan pas je alleen dat ene veldje aan en werkt de automatische bijwerking weer.
  - **Handmatige invoer als laatste redmiddel, zoals gevraagd**: **VVGSD → Stand en schema → "Stand handmatig invoeren"** en **"Wedstrijdschema handmatig invoeren"** zijn schakelaars in de beheerpagina; zet je die aan, dan gebruikt de site de lijst die je daar zelf invult in plaats van de automatisch opgehaalde gegevens (zelfde velden, dus geen dataverlies bij overschakelen). Bewust **nog geen PDF/plaatje-scraper gebouwd**: dat is best wat werk (OCR e.d.) voor een situatie die nu niet bestaat, en de handmatige invoer dekt dat scenario al volledig af — mocht Playpass ooit echt naar een PDF overschakelen, dan bouw ik daar alsnog een aparte importer voor die in dezelfde `data/vvgsd-*-auto.json`-bestanden schrijft, zonder dat de pagina's zelf hoeven te veranderen.
  - **Groepsfoto, topscorers en wedstrijdverslagen zijn en blijven handmatig** (redactionele inhoud, komt nergens automatisch vandaan) — allemaal in te vullen via **VVGSD** in de beheerpagina.
  - **Getest**: de scraper lokaal gedraaid tegen de echte Playpass-pagina (16 teams, 120 wedstrijden, VVGSD's eigen cijfers handmatig gecontroleerd en kloppend: 2 gespeeld, 0-0-2, doelpunten 3-15). Alle drie pagina's met Playwright gecontroleerd op de echte opgehaalde data: geen consolefouten, "eerstvolgende wedstrijd" berekent de juiste eerstvolgende niet-gespeelde wedstrijd, het schema groepeert correct per datum met VVGSD's eigen wedstrijden uitgelicht, de standentabel sorteert en licht VVGSD's rij uit. **Niet getest**: de GitHub Action zelf (moet nog een keer live draaien op GitHub; lokaal alleen het scraper-script getest) en de handmatige-invoer-schakelaars (nog niemand heeft ze aangezet).
  - **Let op na de eerste push**: de GitHub Action heeft schrijfrechten nodig. Check op GitHub onder **Settings → Actions → General → Workflow permissions** of dat op "Read and write permissions" staat; zo niet, zet het aan, anders kan de Action niet automatisch committen.
  - **Seizoensoverdracht**: verandert de Playpass-link (nieuw seizoen/nieuwe poule), pas dan **VVGSD → Stand en schema → "Bron op Playpass"** in de beheerpagina aan — de scraper gebruikt die link, niet een vaste waarde in de code.
- [ ] **Hele site via de beheerpagina bewerkbaar (2026-09-24, op jouw verzoek — nog niet door jou getest)**: nieuwe formulieren **Homepage**, **Verbanden**, **Kasten** en **Veelgestelde vragen**, plus "Teksten op de pagina" bij **Open avonden**, **Besturen** en **VVGSD**, en in **Instellingen** het rode blok "Nieuwsgierig geworden?" (gedeeld door Activiteiten/Besturen/Geschiedenis/Kasten/Verbanden) en de footerteksten.
  - **Hoe het werkt**: teksten staan in `data/paginas/*.json`. `<body data-pagina="paginas/…">` laat `js/site.js` dat bestand laden en alle elementen met `data-inhoud`, `data-inhoud-rijk` (alinea's + opmaak `**vet**`, `[link](adres)`, `~~doorgestreept~~`, `{emailPr}`), `data-inhoud-foto`, `data-inhoud-icoon` en `data-inhoud-lijst` (met `<template>`, ook genest) vullen. Uitleg bovenaan die functie in `js/site.js`. Een leeg veld laat de oorspronkelijke HTML-tekst staan. Pagina's met eigen scripts (FAQ-accordion, kastenkaart) wachten op `VGSD.inhoud`.
  - Bestaande teksten zijn automatisch uit de HTML overgezet (niets overgetypt); alle 11 pagina's gecontroleerd in de browser (zelfde uiterlijk, geen consolefouten, FAQ-accordion en kasten-popup/kaart werken). Beheerpagina in testmodus: alle nieuwe formulieren openen zonder fouten, en elk veld in `config.yml` sluit precies aan op de databestanden (automatisch gecontroleerd).
  - **Update 2026-09-24 (na jouw eerste test)**: elke pagina is in de beheerpagina opgesplitst in losse blokken (klik links op een pagina → rechts direct alle blokken, bv. Homepage → Bovenaan de pagina / Over ons / Pijlers / Leden aan het woord / Instagram). Technisch: één bestand per blok in `data/paginas/<pagina>/` en `data/instellingen/` (was één bestand per pagina; `data/site.json`, `data/geschiedenis.json`, `data/activiteiten-overzicht.json` en `data/vvgsd-instellingen.json` bestaan niet meer). Pagina's noemen hun blokken in `<body data-blokken="...">`. De VVGSD-scraper leest nu `data/paginas/vvgsd/standEnSchema.json` (lokaal getest: 16 teams, 120 wedstrijden). Getest: alle 11 pagina's in de browser, alle 34 blokformulieren in testmodus.
  - **Niet via de beheerpagina**: het menu, de vormgeving, `<title>`/meta-omschrijving per pagina, en een paar kleine vaste knopteksten (bv. "Bekijk foto" op de kastkaartjes, "Terug naar VVGSD"). Zeg het als een daarvan toch bewerkbaar moet.
  - Homepage-foto's van de leden-verhalen staan nog in `images/photos/Persoonlijk verhaal/`; nieuwe uploads komen in `images/photos/leden/`. Nieuwe homepage-foto's in `images/photos/home/`, Instagram-tegels in `images/photos/instagram/`. De hero- en homepagefoto's hebben een gewoon fotoveld (geen bijsnij-kader zoals bij kasten/activiteiten).
  - Niet getest: echt opslaan via GitHub-login (alleen testmodus), en de nieuwe formulieren gevuld met de echte gegevens (testmodus toont lege formulieren).
- [x] **Beheerformulieren opgedeeld in kopjes, op verzoek voor alle pagina's (2026-09-23)**: VVGSD had één lange lijst van ~10 losse velden; nu vier inklapbare kopjes (Groepsfoto, Statistieken, Wedstrijdverslagen, Stand en schema). **Instellingen** had hetzelfde probleem voor het WhatsApp-nummer (stond los boven de al-gegroepeerde Contactgegevens/Sociale media); nu drie kopjes (WhatsApp, Contactgegevens, Sociale media). Dit verandert de vorm van `data/vvgsd-instellingen.json` en `data/site.json` (velden genest onder de kopjes) — `js/site.js` (`VGSD.laadVvgsd()` en `maakWaLink()`) en `scripts/vvgsd-scraper.mjs` zijn daarop aangepast en opnieuw getest. **Overige formulieren aangekeken maar bewust ongewijzigd gelaten**: Activiteiten en Geschiedenis hebben al twee duidelijke kopjes (tekst bovenaan + de lijst); Open avonden en Besturen bestaan uit precies één lijst-veld, dus daar valt niets te scheiden.
- [ ] **ALS HET ECHTE DOMEIN LIVE GAAT (nu nog `preview.vgsd.nl`) — deze plekken moeten dan mee-veranderen:**
  - **DNS + HTTPS-certificaat** voor het nieuwe domein inrichten (zoals eerder gedaan voor `preview.vgsd.nl`: DNS wijzen naar GitHub Pages, dan wachten tot het Let's Encrypt-certificaat er is, dan "Enforce HTTPS" aanzetten in GitHub Settings → Pages).
  - **`CNAME`** (bestand in de root van de repo): staat nu op `preview.vgsd.nl`, moet naar het echte domein.
  - **`admin/config.yml`**: `site_url` en `logo_url` wijzen nog naar `https://preview.vgsd.nl`.
  - **Cloudflare Worker `vgsd-cms-auth`** (dashboard → Settings → Variables and Secrets): `ALLOWED_DOMAINS` staat op `preview.vgsd.nl`; moet het echte domein worden (evt. beide, met een komma ertussen, als preview ook moet blijven werken) — anders weigert de "Inloggen via GitHub"-knop op het nieuwe domein.
  - **GitHub OAuth App** ("VGSD Beheer Login", Settings → Developer settings → OAuth Apps op het `webmasterVGSD`-account): Homepage URL staat nog op `https://preview.vgsd.nl` (cosmetisch, mag bijgewerkt worden). De **Authorization callback URL hoeft niet te wijzigen** — die wijst naar de Cloudflare Worker, niet naar de website zelf.
  - **`BEHEER.md`**: de vermelding van `https://preview.vgsd.nl/admin/` bijwerken naar het echte domein.
- [ ] Activiteitendata controleren (staat in `data/activiteiten.json`, sleutel `openAvonden`; geldt voor de footer op alle pagina's, `open-avonden.html` en het overzicht daar): tijd is Kring en andere maaltijd-activiteiten (Maaltijd en lezing, BBQ en sing-in) op 18:30 gezet, de rest op 20:00. Jaartallen zijn aangenomen als verenigingsjaar 2026-2027 (sept/dec 2026, april/juni 2027). Klopt dit niet, pas het aan per activiteit.
- [ ] Nieuwe activiteiten toevoegen aan diezelfde lijst zodra de kalender wordt aangevuld.
- [ ] Menu herindeling: de volgende nav-items wijzen nu nog naar "#" (placeholder) en moeten gekoppeld worden aan hun echte pagina zodra die bestaat: Fotoalbum, Zakelijk, Log in, en onder "Over ons": Disputen; onder "Contact": Wonen in Delft, Langskomen. ("Activiteiten", "Open avonden", "Kasten (verenigingshuizen)", "Geschiedenis", "Verbanden", "Besturen", "VVGSD (het voetbalteam)" en "Veelgestelde vragen" zijn al gekoppeld.)
- [ ] **Nieuwe pagina `veelgestelde-vragen.html`**: 11 vragen als accordion, tekst overgenomen van de oude website — jij gaf zelf aan dat dit "deels verouderd" is. Op je feedback al bijgewerkt: jaartallen inschrijfdeadline/novitiaat naar 2027, en "13 verenigingen (incl. VGSD) / 12 zusterverenigingen" bij VGS-Nederland toegevoegd. Nog concreet openstaand:
  - **Weekdag bij de inschrijfdeadline nog checken**: "donderdag 20 augustus 2027 23:59" — 20 augustus 2027 valt op een vrijdag, niet op een donderdag. Waarschijnlijk verschuift de exacte OWee-deadline sowieso mee met de OWee 2027-planning; geef de echte datum + tijd door zodra die bekend is, dan klopt de weekdag vanzelf.
  - **Novitiaat-periode "maandag 24 t/m zaterdag 29 augustus 2027" nog checken**: dezelfde weekdag-mismatch (24 augustus 2027 is een dinsdag, geen maandag) — geef de definitieve novitiaat-week voor 2027 door zodra die vaststaat.
  - Twee links wijzen nog naar "#": "hier" (inschrijfformulier) en "dit formulier" (kamer-interesseformulier). Volgens jou verdwijnen deze formulieren waarschijnlijk sowieso — heroverweeg of deze twee stukjes tekst/links dan nog wel moeten blijven staan, of dat ze vervangen moeten worden (bv. door "mail naar pr@vgsd.nl" zonder losse formulierlink) zodra bekend is wat ervoor in de plaats komt.
  - E-mailadres `pr@vgsd.nl` is gebruikt zoals aangeleverd (naast het al bestaande `abactis@vgsd.nl` in de footer) — check of dat de juiste, actieve inbox is.
- [ ] **Nieuwe pagina `verbanden.html`**: de vier verbanden waar de VGSD deel van uitmaakt (zusterverenigingen, VeRa, broederverenigingen, IFES) als vier kaarten, tekst rechtstreeks overgenomen van jou. Nav op alle pagina's gekoppeld. Kleine correctie toegepast: "zustervereningen" → "zusterverenigingen" (typo in de aangeleverde tekst) en "Bijbekringcommissie" → "Bijbelkringcommissie" (vermoedelijke typo) — check of dat klopt. Zusterstedenlijst bevestigd: 12 zusterverenigingen + VGSD zelf = 13 VGS-Nederland in totaal, dat klopt met de aparte lijst op `veelgestelde-vragen.html`.
- [ ] **Pagina `geschiedenis.html`** (2026-09-23 hernoemd van "Geschiedenis, grondslag en doel" naar gewoon "Geschiedenis", overal in de nav): tijdlijn (6 stappen, eind jaren '50 t/m "Vandaag"), tekst overgenomen/samengevat uit de tekst van de oude website die je hebt aangeleverd. Check of de samenvatting in de tijdlijn-titels/koppen (bv. "Onvrede bij de SSR", "Elders ontstaan al VGS'en") de juiste toon/nadruk heeft. Nav op alle pagina's gekoppeld.
  - Laatste stap "Vandaag" (Eén vereniging, veel tradities): "zo'n 30 actieve leden" en de genoemde stromingen (gereformeerd, PKN, evangelisch, katholiek) komen van jou — check of dit nog klopt en of de lijst met stromingen compleet is.
  - **Jaartal ontbreekt**: de laatste tijdlijnstap ("Open voor alle christenen" — de vereniging was aanvankelijk alleen voor gereformeerde studenten en is op enig moment opengesteld voor christenen uit alle richtingen) staat er nu zonder jaartal in ("Jaartal volgt"). Geef het jaartal door zodra bekend, dan vul ik het in.
  - **Grondslag en doel (2026-09-23, op jouw verzoek)**: de aparte "Grondslag en doel"-sectie (met de 1961-grondslagtekst die mogelijk verouderd was, zie eerdere notitie hierover) is helemaal verwijderd. Het doel is verwerkt in de tijdlijnstap "23 maart 1961 — De VGSD is een feit" ("...met als doel: de onderlinge vorming van studenten tot christenacademicus — óók door de techniek te leren verstaan in het licht van Gods Woord."), zonder de grondslag-tekst. Check of deze doelformulering nog klopt met de huidige statuten.
  - **Data-gestuurd gemaakt (2026-09-23)**: tekst en tijdlijn staan nu in `data/geschiedenis.json` en zijn te bewerken via **Geschiedenis** in de beheerpagina — een jaartal aanvullen of een tijdlijnstap toevoegen kan dus voortaan zonder code-wijziging.
- [x] **`activiteiten.html`, rode highlight-kaarten (2026-09-23, op jouw verzoek)**: de kaarten Bijbelkring, Avond van Zang en Gebed, PaasCongres en Vrijwilligers zijn rood+wit gestyled (bg-brand-600, tekst warm-50/brand-100) om de christelijke activiteiten uit te lichten tegen de verder witte pagina; de overige kaarten blijven ongewijzigd.
- [x] **`activiteiten.html`, volgorde herschikt (2026-09-23, op jouw verzoek)**: de vier rode kaarten stonden eerst twee-aan-twee geclusterd (Bijbelkring+AZG bovenaan, PaasCongres+Vrijwilligers onderaan); nu verspreid over rijen 1/2/3/5 in wisselende kolommen, en PaasCongres/Vrijwilligers staan veel hoger. Als extra promotie zijn Dies Natalis en Lustrum (de twee grootste evenementen) meteen vooraan gezet. Volgorde is niet vastgezet — schuif gerust verder als iets anders beter promoot. Kaarten zonder foto (PaasCongres, Vrijwilligers, Kringweekend) hebben `self-start` gekregen zodat ze niet meer uitgerekt worden tot de hoogte van foto-kaarten in dezelfde rij (dat gaf lege ruimte onderin).
- [ ] `activiteiten.html`: omschrijving van "Vrijwilligers" invullen zodra bekend (nu een placeholder-kaart met "Omschrijving volgt"). Kan sinds 2026-09-23 direct via **Activiteiten** in de beheerpagina, geen code-wijziging meer nodig.
- [ ] `activiteiten.html`, kaart Weekenden: laatste zin over het PaasCongres leek af te breken bij "...een weekend lang de amicaliteit." Aangevuld met "te beleven", graag checken of dat de bedoeling was.
- [ ] Foto's per open avond toevoegen op de nieuwe `open-avonden.html` (elke open avond heeft nu nog alleen een icoon, geen foto). Foto's voor `activiteiten.html` zijn al verwerkt (uit `images/photos/activiteiten/`).
- [ ] Fotokeuze per kaart op `activiteiten.html` nog checken: bij "Werkgroepen" is de foto (iemand achter een keyboard/mixer) een educated guess op basis van de bestandsnaam.
- [ ] Weekenden zijn nu opgesplitst in 5 losse kaarten (Eerstejaarsweekend, Zeilweekend, Kringweekend, Liftweekend, PaasCongres). Gekozen foto's zijn `EJW-1.jpg` en `Liftweekend-1-scaled.jpg` (eerste van de map) — check of dit de beste foto's zijn, er staan meer opties in `images/photos/activiteiten/EJW/` en `.../Liftweekend/`. Kringweekend en PaasCongres hebben nog geen foto (geen materiaal beschikbaar) en tonen nu alleen een icoon.
- [ ] `activiteiten.html`, kaart Soos: huidige foto (`ThemaSoosch-JG-21-5.jpg`) is prima voor nu, maar op termijn een nieuwe foto zoeken/aanleveren.
- [ ] `activiteiten.html`, kaart Lustrum: huidige foto (`Lustrum/Lustrum-Reis-Porto3-scaled.jpg`) vervangen zodra er een nieuwe lustrumfoto beschikbaar is.
- [ ] WhatsApp-nummer (06 33 89 19 80, uit de footer): wordt nu overal gebruikt — de zwevende knop rechtsonder én alle "Meld je aan"-links voor open avonden (footer-widget, CTA-band en jaaroverzicht op `open-avonden.html`). Vervang op één centrale plek zodra bekend is welk nummer echt voor WhatsApp-contact met (potentiële) leden gebruikt gaat worden: `whatsapp` in `data/site.json` (internationaal formaat zonder plus, bv. `31633891980`; los van het telefoonnummer in de footer, dat staat bij `contact.telefoon`).
- [ ] Homepage, sectie "Leden aan het woord": Sven van der Ploeg en Elias Hoste zijn gekoppeld aan `IMG_9979-scaled.jpg` en `IMG_7718-scaled-e1714345628433.jpg` in `images/photos/Persoonlijk verhaal/` op basis van een aanname die door de gebruiker is bevestigd — check dit nog eens als er twijfel is. "Lid sinds"-jaartal ontbreekt nog (was niet aangeleverd); toevoegen indien gewenst.
- [ ] Homepage, sectie "Volg ons op Instagram": nu een placeholder-grid van 6 lege tegels. Foto's toevoegen kan sinds 2026-09-24 via **Homepage → Instagram → Foto's** (bewust geen live embed). De knop gebruikt nu de Instagram-link uit Instellingen.
- [ ] **Nieuwe pagina `fotoalbum.html`** (nav-item "Fotoalbum" staat al klaar, wijst nu naar "#"): nog geen concept gekozen. Voorheen (oude site) was het een ongesorteerde sfeerimpressie van willekeurige foto's door elkaar — besproken alternatief was een losse masonry-grid met daarbovenop optionele filterknoppen per soort activiteit (Kringen/Weekenden/Feesten/etc.), zodat het zowel een sfeervolle eerste indruk geeft (belangrijk voor werving van nieuwe leden) als doorzoekbaar is. Gebruiker weet nog niet wat ze willen — pas bouwen na akkoord over de aanpak.
- [ ] **Pagina `kasten.html`** (interactieve kaart met de 6 kasten, per jouw verzoek): foto's en tekst van alle 6 kasten (incl. Halte Boszicht) zijn nu verwerkt. Nog openstaand:
  - Halte Boszicht-tekst is overgenomen uit `images/photos/Kasten/Halte Boszicht (foto + tekst).png` (een grappige "bewonerskaart" met doorgestreepte clichés). Doorstrepen is met `<s>` tags in de `tekst`-array gedaan (modal rendert nu `innerHTML` i.p.v. `textContent`). De slotzin ("...feliciteren de vereniging...") en de handtekeningen zijn op verzoek weer verwijderd uit de kaart.
  - Fotobijsnijding: `fotoPositie` (CSS `object-position`) toegevoegd per kast waar nodig — Halte Boszicht en Fort Juliana staan op `center 20%`/`center 25%` (foto omhoog), Het Oostblok op `center 65%` (foto omlaag). Andere kasten hebben geen `fotoPositie` (blijft `center`); pas gerust bij als het net niet goed staat.
  - "Het Fort" is hernoemd naar "Fort Juliana" (naam uit de aangeleverde tekst) — zeg het als dat niet de bedoeling was.
  - Op verzoek verwijderd: de privacy-disclaimerzin onder de intro ("De pins geven de buurt aan, niet het exacte adres...") en de locatieregel (`gebied`, bv. "Bij het Wilhelminapark") die eerder op elk kaartje én in de lightbox-modal stond. `gebied` staat nog wel als veld in de `kasten`-array (ongebruikt in de weergave) — kan eventueel later nog dienen voor bv. een tooltip op de kaart-pins.
  - De foto `images/photos/Kasten/BaSIS pagina lustrumbundel.png` was 16,6 MB (7048×4176); daarvan is een verkleinde versie gemaakt (`BaSIS-lustrumbundel-scaled.jpg`, 1600px breed) die nu op de site staat. Het originele bestand staat nog in dezelfde map als bronmateriaal.
  - Kaartposities: coördinaten zijn een schatting op basis van de buurt die je noemde (privacy, geen exact adres). De BWS en Het Oostblok zijn al bijgesteld op jouw feedback; check de overige 4 pins ook nog eens en schuif zo nodig `lat`/`lon` bij in de `kasten`-lijst in `kasten.html`.
- [ ] **Nieuwe pagina `besturen.html`**: het huidige bestuur (Bulthuis) groot uitgelicht bovenaan, daaronder alle overige 66 besturen (terug tot oprichting 1961) als kaartjes-grid + lightbox-modal, zelfde stijl als `kasten.html`. Nav op alle pagina's gekoppeld. Nog openstaand:
  - **Foto's (2026-09-21 bijgewerkt)**: 28 van de 31 foto's zijn verwerkt en staan in `images/photos/besturen/` (PNG/JPEG geconverteerd naar `.jpg`, en alles breder dan 1600px verkleind/herbecomprimeerd — origineel ~27MB aan bestanden, nu ~6,6MB totaal).
    - **Data staan nu in `data/besturen.json` (sinds 2026-09-21)**, niet meer in `besturen.html`. Per bestuur: `naam`, `praeses`, `abactis`, `fiscus`, `assessor`, `foto` (`{src, positie}`), `quote`, `tekst`. De pagina leest alleen wat in dat bestand staat. **Let op: een foto in de map zetten is niet meer genoeg** — het pad moet ook als `foto.src` bij dat bestuur in `data/besturen.json` staan (dat gaat via de beheerpagina). Zonder `foto` verdwijnt het fotovlak.
    - **Nog geen foto (3x, bevestigd door gebruiker)**: "de Jong III" (huidig bestuur — foto moet nog gemaakt worden), "Sonneveld", "Burema". Kaarten zonder foto tonen géén placeholder (fotovlak verdwijnt); alleen het huidige bestuur (eerste in `besturen.json`) toont een placeholder-icoon zolang het geen foto heeft. Sonneveld en Burema tonen dus nu geen fotovlak.
    - **Tigelaar**: enige beschikbare foto is laagresolutie (195×260px) — gebruiker heeft geen grotere versie, bewust zo gebruikt.
    - **Kaartindeling (2026-09-21)**: kaarten zijn nu een subgrid van 4 rijen (foto / titel / namen / "Lees meer"), zodat per rij de titels, namen en "Lees meer" op dezelfde hoogte staan, ongeacht aantal bestuursleden of "Lees meer". De witte strook boven/onder sommige foto's kwam doordat kaarten een `<button>` waren die zijn inhoud verticaal centreert; dat is opgelost. Gemeten: 22 kaartenrijen, geen enkele met hoogteverschil. Nadeel: in de rij met Sonneveld en Burema (zonder foto, naast Timmer met foto) blijft boven hun titel een leeg fotovlak staan, om de titels op één lijn te houden. Ontbrekende foto's aanleveren lost dat vanzelf op.
    - **Laden per batch + vloeiendere animatie (2026-09-21)**: het archief laadt niet meer in één keer maar per 12 kaarten terwijl je scrolt (`laadArchiefTijdensScrollen` in `besturen.html`, batch-grootte `BATCH`, voorsprong `VOORSPRONG`). De haperige overgang kwam doordat `.reveal` (fade) en `.card-tilt` (hover-lift) elkaars `transition` overschreven, waardoor de fade abrupt was; kaarten hebben nu een eigen animatie (fade + 14px omhoog, 0,52s, lichte stagger per kolom) en schakelen daarna terug naar de snelle hover-animatie. Laden en tonen lopen via één scroll-meting per beeldframe (bewust geen IntersectionObserver: dat bleek in de testomgeving wispelturig). Getest: 12→24→36→48→66 kaarten, uitlijning per rij ongewijzigd. **De vloeiendheid zelf kon ik in de testomgeving niet meten (te trage beeldframes) — beoordeel dit in een echte browser.** NB: dezelfde `.reveal` + `.card-tilt` botsing bestaat waarschijnlijk ook op andere pagina's (bv. `activiteiten.html`, `open-avonden.html`) waar kaarten beide klassen hebben.
    - **Foto-uitlijning**: alle foto's zijn `object-fit: cover` in een vak van 12rem hoog, standaard uitgelijnd op `50% 30%` (gezichten zitten meestal in de bovenste helft). De uitlijning staat per foto in `foto.positie` in `data/besturen.json` en stel je in met het sleep-en-bijsnij-veld (Gunnink, de Jong en Tigelaar hebben een afwijkende waarde); pas dat aan als er nog foto's zijn waar hoofden wegvallen.
  - **Vanaf "Triemstra" (en alle oudere besturen daarna) bestaan er geen foto's** — dat is per jouw bericht bevestigd; die besturen hebben in `data/besturen.json` gewoon geen `foto`-veld en tonen dus geen fotovlak.
  - **Aanname te checken**: bij "Bestuur de Jong III" (bovenste, meest recente kaartje) waren geen functies (Praeses/Abactis/Fiscus) expliciet bij de namen gegeven, alleen "J. de Jong, H.H.M. Luhrman & E.J. Groen". Ik heb deze volgorde overgenomen als Praeses/Abactis/Fiscus, consistent met alle andere besturen — check of dat klopt.
  - **Aanname te checken**: bij "Bestuur Triemstra" ontbreekt de voornaam/het initiaal van de praeses (aangeleverd als "? Triemstra"); dit staat nu letterlijk zo op de pagina. Geef door zodra bekend.
  - **Kleine correcties toegepast** (mogelijk tekstfouten uit de bron-tekst, check of dit klopt): bij "Bestuur Gunnink" was er een dubbel vraagteken ("geweest? ?") — samengevoegd tot één vraagteken. Bij "Bestuur de Jong" (de oudste met een uitgebreide tekst) stonden er "?"-tekens op plekken waar vermoedelijk een apostrof ("foto's") en een ë ("poriën") hoorden te staan — dat is zo hersteld.
- [ ] **Nieuwe architectuur: hybride Astro + headless WordPress** (vervangt het eerdere "één zelfgebouwd WP-thema"-plan). Front-end staat nu in `astro-site/` (Astro + TypeScript + Tailwind v4, SSG, bouwt en draait al met mock-data — zie `astro-site/README.md`). Nog te doen:
  - WordPress-backend live zetten op een subdomein (bv. `api.vgsd.nl`) puur als headless content-API, zonder page builder en zonder plugins (ook geen ACF, per gebruikersvoorkeur) — custom post types + handmatig gecodede meta boxes voor "nieuws"/pagina's.
  - Apart, besloten ledenportaal op een ander subdomein (bv. `leden.vgsd.nl`), login/permissies/formulieren volledig via WordPress zelf — de Astro-site linkt er alleen naartoe via de "Leden inlog"-knop in `astro-site/src/components/Header.astro`.
  - Zodra die backend bestaat: `astro-site/.env` invullen (`WORDPRESS_API_URL`, `WORDPRESS_MEMBERS_URL`) — de mock-data in `astro-site/src/lib/mock-data.ts` wordt dan automatisch niet meer gebruikt, geen codewijzigingen nodig.
  - Hosting voor de Astro-front-end kiezen en inrichten (Cloudflare Pages of Vercel, zie README).
  - Rebuild-trigger inrichten (bv. WordPress-webhook die een nieuwe Cloudflare/Vercel-deploy start), want de front-end is statisch en moet opnieuw gebouwd worden bij nieuwe WP-content.
  - De oude statische site (`index.html`, `activiteiten.html`, `open-avonden.html` in de repo-root) blijft voorlopig staan als referentie/fallback; opruimen zodra de Astro-site alle content en pagina's dekt.
