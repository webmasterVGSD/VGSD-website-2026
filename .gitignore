# Local testing artifacts, not part of the deployed site
screenshots/

# Astro build tooling (kept out of the static-site deploy for now)
astro-site/node_modules/
astro-site/dist/
astro-site/.env

# Unused source material (59MB, not referenced by any page — see TODO.md)
images/photos/Kasten/Halte Boszicht (foto + tekst).png

# OS cruft
.DS_Store
Thumbs.db

# Local tool config/logs, not part of the site
.claude/
.playwright-mcp/
.agents/
